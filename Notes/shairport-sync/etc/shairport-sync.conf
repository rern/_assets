general = {
	name = "rAudio";
	run_this_when_volume_is_set = "/usr/bin/sudo /srv/http/bash/cmd.sh volumepushstream";
}
sessioncontrol = {
	run_this_before_play_begins = "/usr/bin/sudo /srv/http/bash/cmd.sh shairport";
}
alsa = {
	output_device = "hw:1,0";
	mixer_control_name = "PCM";
}
metadata = {
  enabled = "no";
};
dbus = {
  mpris = "yes";
};
